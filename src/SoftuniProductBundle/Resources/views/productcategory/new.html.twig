{% extends 'base.html.twig' %}

{% block title %}
    Add new category
{% endblock %}

{% block body %}
    <h1>Add new product category</h1>
    <div class="w3-margin-bottom">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="w3-btn w3-green w3-round-large w3-margin-top" type="submit" value="Create" />
    {{ form_end(form) }}
    </div>
    <a class="w3-btn w3-blue w3-round-large" href="{{ path('admin_product-category_index') }}">Back to the list</a>

{% endblock %}
